# 🏠 Room Analyzer - ระบบวิเคราะห์และให้คะแนนห้องพัก

เครื่องมือช่วยตัดสินใจเลือกที่พักอาศัย โดยใช้หลักการวิเคราะห์ข้อมูลและให้คะแนนตามน้ำหนักความสำคัญที่คุณกำหนดเอง (Weighted Scoring System)

## ✨ คุณสมบัติเด่น
* **Custom Weights:** กำหนดได้เองว่าคุณให้ความสำคัญกับอะไรมากกว่ากัน (เช่น บางคนเน้นใกล้ MRT, บางคนเน้นพื้นที่ทำงาน)
* **Cost Calculator:** ประมาณการค่าใช้จ่ายจริงรวมค่าน้ำ ค่าไฟ และค่าซักผ้ารายเดือน
* **Comparison Dashboard:** เปรียบเทียบห้องพักแต่ละที่และจัดอันดับให้อัตโนมัติ
* **Data Portability:** สามารถ Copy ข้อมูลห้องพักในรูปแบบ JSON เพื่อส่งต่อหรือสำรองข้อมูลไว้ได้

## 🚀 วิธีการใช้งาน
1.  **ติดตั้ง dependencies:** (ใช้แค่ Flask)
    ```bash
    pip install flask
    ```
2.  **เริ่มใช้งาน:** รันไฟล์ `app.py`
    ```bash
    python app.py
    ```
3.  **ตั้งค่า (Settings):** กดที่ปุ่ม "แสดง/ซ่อน" ในส่วนการตั้งค่าเพื่อกำหนดงบประมาณและค่าน้ำหนักคะแนน
4.  **เพิ่มข้อมูล:** กดปุ่ม "เพิ่มห้องพักใหม่" และกรอกข้อมูลตามความเป็นจริง
5.  **ดูผลลัพธ์:** ระบบจะคำนวณคะแนนรวมและอันดับให้ทันทีด้านล่างสุด

## 📊 เกณฑ์การให้คะแนน
ระบบจะนำข้อมูลที่คุณกรอกไปคำนวณผ่านสูตรคณิตศาสตร์ เช่น:
- **ราคา:** ยิ่งราคาถูกกว่างบ ยิ่งได้คะแนนสูง
- **ระยะทาง:** ยิ่งใกล้ MRT หรือร้านสะดวกซื้อ คะแนนยิ่งเพิ่มขึ้น
- **สิ่งอำนวยความสะดวก:** ให้คะแนนตามออปชันที่เลือก (แอร์, น้ำอุ่น, ครัว ฯลฯ)

---
*พัฒนาด้วย Python (Flask) และ Vanilla JavaScript*